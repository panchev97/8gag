{% extends 'base.html.twig' %}

{% block body %}
    <div style="text-align: center;">
        <div class="well">
            <h1>Edit Picture</h1>
                <hr>
                    <div class="container">
                        <div class="col-md-4 col-md-offset-4">
                            {{ form_start(editPictureForm) }}
                                {{ form_widget(editPictureForm) }}
                                     <button class="btn btn-primary" type="submit">Edit Picture</button>
                            {{ form_end(editPictureForm) }}
                        </div>
                    </div>
        </div>
    </div>
{% endblock %}